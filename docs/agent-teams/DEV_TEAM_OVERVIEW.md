# 개발팀 개요 (Development Team Overview)

## 👤 역할 정의

부동산 매물 분석 웹 애플리케이션을 설계, 개발, 운영하는 **개발팀**의 전체 구조와 협업 방식을 정의합니다.
5명의 전문 개발 에이전트(PM, 디자이너, 프론트엔드, 백엔드, 데이터 엔지니어)가 협업하여
"네이버페이 부동산"과 유사한 매물 분석 플랫폼을 구축합니다.

---

## 🎯 핵심 책임

### 1. 웹 애플리케이션 개발
- 매물 검색, 지도 기반 탐색, 상세 분석 리포트 표시
- 반응형 디자인 (데스크톱 + 모바일)
- 실시간 데이터 업데이트 및 알림

### 2. 분석팀 연동
- 5개 분석팀(시세/입지/투자/규제/리스크) 결과를 웹에 표시
- 분석 요청 → 처리 → 결과 표시 파이프라인
- 분석 데이터 시각화 (레이더 차트, 비교표 등)

### 3. 데이터 파이프라인
- 공공 API 기반 매물·실거래가 데이터 수집
- Geocoding 및 공간 데이터 처리
- 분석팀에 데이터 패키지 전달

### 4. 인프라 운영
- Vercel 배포 및 CI/CD
- Supabase 데이터베이스 관리
- 모니터링 및 장애 대응

---

## 🛠️ 기술 스택 & 도구

### 프론트엔드
| 기술 | 용도 | 버전 |
|------|------|------|
| **Next.js** | App Router 기반 풀스택 프레임워크 | 14+ |
| **TypeScript** | 타입 안전성 | 5.x |
| **Tailwind CSS** | 유틸리티 기반 스타일링 | 3.x |
| **react-kakao-maps-sdk** | 카카오맵 React 래퍼 | 1.x |
| **Recharts** | 분석 데이터 차트 | 2.x |
| **Zustand** | 클라이언트 상태 관리 | 4.x |
| **TanStack Query** | 서버 상태 관리 | 5.x |
| **nuqs** | URL 상태 동기화 | 1.x |

### 백엔드
| 기술 | 용도 | 버전 |
|------|------|------|
| **Supabase** | BaaS (DB + Auth + Storage + Realtime) | - |
| **PostgreSQL + PostGIS** | 공간 데이터 지원 RDBMS | 15+ |
| **Supabase Edge Functions** | 서버리스 API (Deno) | - |
| **pg_cron** | DB 내 스케줄링 | - |

### 인프라
| 기술 | 용도 |
|------|------|
| **Vercel** | 프론트엔드 호스팅 + Edge Functions |
| **Supabase Cloud** | 데이터베이스 호스팅 |
| **GitHub Actions** | CI/CD 파이프라인 |

### 외부 API
| API | 용도 |
|-----|------|
| **카카오맵 JavaScript SDK** | 지도 표시, 마커, 클러스터링 |
| **카카오 로컬 API** | Geocoding, 키워드 검색 |
| **국토교통부 실거래가 API** | 아파트 매매 실거래 데이터 |
| **학교알리미 API** | 학교 정보 (학군) |
| **에어코리아 API** | 대기질 정보 |

---

## 🔧 개발 프로세스

### 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                        사용자 (브라우저)                          │
└──────────────────────────────┬──────────────────────────────────┘
                               │ HTTPS
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Vercel (Edge Network)                        │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │              Next.js App Router (SSR/SSG)                │    │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────────┐  │    │
│  │  │  /       │ │ /search  │ │/property │ │ /compare  │  │    │
│  │  │  홈      │ │ 검색     │ │ 상세     │ │ 비교      │  │    │
│  │  └──────────┘ └──────────┘ └──────────┘ └───────────┘  │    │
│  └─────────────────────────────────────────────────────────┘    │
└──────────────────────────────┬──────────────────────────────────┘
                               │ supabase-js / Edge Functions
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Supabase Cloud                               │
│  ┌──────────────┐ ┌──────────────┐ ┌────────────────────────┐  │
│  │ PostgreSQL   │ │    Auth      │ │   Edge Functions       │  │
│  │ + PostGIS    │ │  (GoTrue)    │ │  - search-properties   │  │
│  │              │ │              │ │  - analysis-request    │  │
│  │ - properties │ │ - Email/PW   │ │  - proxy (공공API)     │  │
│  │ - analysis_  │ │ - OAuth      │ └────────────────────────┘  │
│  │   reports    │ │              │ ┌────────────────────────┐  │
│  │ - profiles   │ └──────────────┘ │   Realtime             │  │
│  │ - favorites  │                   │  - 분석 진행 상태       │  │
│  └──────────────┘                   └────────────────────────┘  │
└──────────────────────────────┬──────────────────────────────────┘
                               │ HTTP / pg_cron
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                      외부 데이터 소스                             │
│  ┌──────────────┐ ┌──────────────┐ ┌────────────────────────┐  │
│  │ 국토교통부    │ │ 카카오맵 API │ │ 학교알리미 / 에어코리아  │  │
│  │ 실거래가 API  │ │ Geocoding   │ │ 공공데이터포털           │  │
│  └──────────────┘ └──────────────┘ └────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 개발 워크플로우

```
1. PM이 PRD & 사용자 스토리 작성
2. 디자이너가 와이어프레임 & 디자인 시스템 구축
3. 백엔드가 DB 스키마 & API 설계
4. 프론트엔드가 컴포넌트 개발 & API 연동
5. 데이터 엔지니어가 수집 파이프라인 구축
6. 통합 테스트 & QA
7. Vercel 배포
```

---

## 📄 산출물 예시

### 개발팀 산출물 목록

| 담당자 | 산출물 | 형식 |
|--------|--------|------|
| PM | PRD, 사용자 스토리, 페이지 구조 | Markdown |
| 디자이너 | 디자인 시스템, 와이어프레임, 컴포넌트 명세 | Markdown + Tailwind Config |
| 프론트엔드 | React 컴포넌트, 페이지, Hooks | TypeScript/TSX |
| 백엔드 | DB 스키마, RLS 정책, Edge Functions | SQL + TypeScript |
| 데이터 엔지니어 | 수집 스크립트, 스케줄링, 데이터 인터페이스 | TypeScript + SQL |

---

## 🤝 팀원 간 협업

### 개발팀 내부 협업

```
[PM] ──PRD/스토리──────> [Designer] ──디자인토큰──> [Frontend]
 │                                                     ↑
 ├──인터페이스 규격────> [Backend] ──스키마/타입─────────┘
 │                         │
 └──분석 규격──────────> [Data Engineer] ──데이터 파이프라인
```

### 분석팀과의 인터페이스

개발팀은 분석팀(시세/입지/투자/규제/리스크)에 데이터를 제공하고, 분석 결과를 웹에 표시합니다.

#### 분석 요청 타입

```typescript
interface AnalysisRequest {
  id: string;
  property_id: string;
  user_id: string;
  analysis_types: AnalysisType[];
  status: 'pending' | 'processing' | 'completed' | 'failed';
  created_at: string;
  completed_at: string | null;
}

type AnalysisType = 'market' | 'location' | 'investment' | 'regulation' | 'risk';
```

#### 분석 결과 타입

```typescript
interface AnalysisReport {
  id: string;
  request_id: string;
  property_id: string;
  analysis_type: AnalysisType;
  score: number;           // 0-100
  grade: string;           // A+, A, B+, B, C+, C, D, F
  summary: string;
  details: Record<string, any>;
  strengths: string[];
  weaknesses: string[];
  created_at: string;
}
```

#### analysis_reports 테이블

```sql
CREATE TABLE analysis_reports (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  request_id UUID REFERENCES analysis_requests(id),
  property_id UUID REFERENCES properties(id),
  analysis_type TEXT NOT NULL,
  score INTEGER CHECK (score >= 0 AND score <= 100),
  grade TEXT NOT NULL,
  summary TEXT NOT NULL,
  details JSONB DEFAULT '{}',
  strengths TEXT[] DEFAULT '{}',
  weaknesses TEXT[] DEFAULT '{}',
  created_at TIMESTAMPTZ DEFAULT now()
);
```

---

## ⚠️ 주의사항

### 기술적 주의사항
- 카카오맵 API 키는 환경변수로 관리, 클라이언트 노출 시 도메인 제한 필수
- Supabase RLS를 반드시 활성화하여 데이터 접근 제어
- 공공 API 호출 횟수 제한(일일 1,000건 등)에 유의
- PostGIS 공간 쿼리는 인덱스 필수 (성능 10배 차이)

### 프로세스 주의사항
- PM의 PRD가 확정되기 전에 개발 착수하지 않기
- 디자인 토큰이 코드와 항상 동기화되어야 함
- 분석팀 인터페이스 타입 변경 시 모든 관련 문서 동시 업데이트

### 보안 주의사항
- API 키·시크릿은 절대 클라이언트 코드에 포함하지 않기
- Supabase Service Role Key는 서버 사이드에서만 사용
- 사용자 입력은 항상 서버에서 검증 (XSS, SQL Injection 방지)

---

## 📈 고급 구현 패턴

### 1. 분석 요청 비동기 처리

```
사용자 → "분석 요청" 클릭
  → Edge Function: analysis_requests INSERT (status: 'pending')
  → Realtime 구독으로 프론트엔드에 진행 상태 실시간 표시
  → 분석팀 에이전트 실행 (백그라운드)
  → 완료 시 analysis_reports INSERT + status 업데이트
  → 프론트엔드 자동 갱신
```

### 2. 지도 기반 매물 검색 최적화

```
지도 이동/줌 → debounce(300ms)
  → bounds 좌표 추출
  → PostGIS RPC: search_properties_in_bounds(sw_lat, sw_lng, ne_lat, ne_lng)
  → React Query 캐싱 (staleTime: 30초)
  → 마커 클러스터링 (줌 레벨별)
```

### 3. ISR (Incremental Static Regeneration)

```
매물 상세 페이지: revalidate = 3600 (1시간)
검색 페이지: 클라이언트 사이드 렌더링 (동적)
홈: revalidate = 86400 (24시간)
```

---

## 📝 체크리스트

### 프로젝트 시작 전 확인

- [ ] 기술 스택 최종 확정 (Next.js + Supabase + 카카오맵)
- [ ] 카카오 개발자 앱 등록 및 API 키 발급
- [ ] Supabase 프로젝트 생성 및 PostGIS 확장 활성화
- [ ] Vercel 프로젝트 연결
- [ ] 공공데이터포털 API 키 발급 (국토부, 학교알리미, 에어코리아)
- [ ] GitHub 리포지토리 및 브랜치 전략 설정
- [ ] 환경변수 설정 (.env.local)
- [ ] 분석팀 인터페이스 타입 합의
- [ ] 디자인 시스템 기본 토큰 확정
- [ ] MVP 범위 최종 합의

---

## 🎓 예상 질문 & 답변

**Q: 왜 Next.js + Supabase 조합인가요?**
A: Next.js는 SSR/SSG/ISR을 모두 지원하여 SEO와 초기 로딩 성능이 우수합니다. Supabase는 PostgreSQL 기반이라 PostGIS 공간 쿼리를 바로 사용할 수 있고, Auth/Realtime/Edge Functions까지 올인원으로 제공합니다.

**Q: 카카오맵 대신 네이버 지도나 Google Maps는?**
A: 카카오맵은 국내 주소 검색과 POI 데이터가 가장 정확하고, React 공식 SDK가 잘 관리되고 있습니다. 부동산 앱 특성상 국내 데이터 정확도가 핵심입니다.

**Q: 분석팀 결과는 어떻게 전달되나요?**
A: `analysis_requests` 테이블에 요청을 INSERT하면, 분석팀 에이전트가 이를 처리하고 `analysis_reports` 테이블에 결과를 INSERT합니다. 프론트엔드는 Supabase Realtime으로 변경을 구독합니다.

**Q: 6주면 MVP가 가능한가요?**
A: 가능합니다. 1주차 기획, 2주차 기반 구축(DB+인증+지도), 3-4주차 핵심 기능(검색+상세+분석연동), 5주차 통합 테스트, 6주차 배포로 구성합니다.

---

## 🔗 참고 자료

### 개발팀 상세 역할 문서
- [프로덕트 매니저](./PRODUCT_MANAGER.md)
- [UI/UX 디자이너](./UI_UX_DESIGNER.md)
- [프론트엔드 개발자](./FRONTEND_DEVELOPER.md)
- [백엔드 개발자](./BACKEND_DEVELOPER.md)
- [데이터 엔지니어](./DATA_ENGINEER.md)

### 분석팀 역할 문서
- [시세 분석가](./MARKET_ANALYST.md)
- [입지 분석가](./LOCATION_ANALYST.md)
- [투자 분석가](./INVESTMENT_ANALYST.md)
- [규제 분석가](./REGULATION_ANALYST.md)
- [리스크 분석가](./RISK_ANALYST.md)

### 공식 문서
- [Next.js 공식 문서](https://nextjs.org/docs)
- [Supabase 공식 문서](https://supabase.com/docs)
- [카카오맵 API 문서](https://apis.map.kakao.com/)
- [Vercel 공식 문서](https://vercel.com/docs)

---

## 📅 6주 개발 로드맵

| 주차 | 단계 | 핵심 작업 | 담당 |
|------|------|----------|------|
| 1주차 | 기획 | PRD 작성, 디자인 시스템, DB 스키마 설계 | PM, 디자이너, 백엔드 |
| 2주차 | 기반 구축 | Next.js 프로젝트 셋업, Supabase 연동, 인증, 카카오맵 기본 연동 | 프론트엔드, 백엔드 |
| 3주차 | 핵심 기능 (1) | 매물 검색, 지도 탐색, 매물 상세 페이지 | 프론트엔드, 백엔드 |
| 4주차 | 핵심 기능 (2) | 분석 요청/결과 표시, 비교 기능, 관심 매물 | 프론트엔드, 데이터 |
| 5주차 | 데이터 & 통합 | 공공 API 수집 파이프라인, 분석팀 연동 테스트 | 데이터, 전체 |
| 6주차 | 배포 | 성능 최적화, QA, Vercel 배포, 모니터링 | 전체 |

---

**역할 버전**: 1.0
**최종 수정일**: 2026-02-15
**담당 영역**: 개발팀 전체 총괄
**협업 팀원**: PM, UI/UX 디자이너, 프론트엔드 개발자, 백엔드 개발자, 데이터 엔지니어
